---
id: path-builder
title: "PathBuilder"
description: "TypeScript API reference for PathBuilder. Builder for creating vector paths in PDF documents. Supports creating paths with: - Lines - Bezier curves - Custom stroke and fill colors - Stroke width and dash patterns"
pagination_next: null
pagination_prev: null
---

<!-- Generated by scripts/generate-v3-reference.js. Do not edit manually. -->

# `PathBuilder`

**Kind:** class  
**Module/package:** `path-builder`  
**SDK:** `pdfdancer-client-typescript 3.0.1`

Builder for creating vector paths in PDF documents.

Supports creating paths with:
- Lines
- Bezier curves
- Custom stroke and fill colors
- Stroke width and dash patterns

## Declaration

```typescript
class PathBuilder
```

## Members

### `constructor`

```typescript
constructor(_client: PDFDancer, _defaultPageNumber?: number): PathBuilder
```

### `add`

```typescript
add(): Promise<boolean>
```

Add the path to the PDF document.

### `addSegment`

```typescript
addSegment(segment: PathSegment): PathBuilder
```

Add a custom path segment.

### `at`

```typescript
at(pageNumber: number, x: number, y: number): PathBuilder
```

Set the position where the path will be added.
Can be called with (x, y) for page-level or (pageNumber, x, y) for document-level.

### `at`

```typescript
at(x: number, y: number): PathBuilder
```

Set the position where the path will be added.
Can be called with (x, y) for page-level or (pageNumber, x, y) for document-level.

### `atPosition`

```typescript
atPosition(position: Position): PathBuilder
```

Set the position using a Position object.

### `bezierTo`

```typescript
bezierTo(cp1x: number, cp1y: number, cp2x: number, cp2y: number, x: number, y: number): PathBuilder
```

Draw a cubic Bezier curve from the current point.

### `circle`

```typescript
circle(cx: number, cy: number, radius: number): PathBuilder
```

### `closePath`

```typescript
closePath(): PathBuilder
```

### `dashPattern`

```typescript
dashPattern(dashArray: number[], dashPhase?: number): PathBuilder
```

Set the dash pattern for subsequent path segments.

### `evenOddFill`

```typescript
evenOddFill(useEvenOdd: boolean): PathBuilder
```

Set whether to use even-odd fill rule (true) or nonzero winding rule (false).

### `fillColor`

```typescript
fillColor(color: Color): PathBuilder
```

Set the fill color for subsequent path segments.

### `lineTo`

```typescript
lineTo(x: number, y: number): PathBuilder
```

Draw a line from the current point to the specified point.

### `moveTo`

```typescript
moveTo(x: number, y: number): PathBuilder
```

Move to a point without drawing.
Sets the current point for subsequent drawing operations.

### `rectangle`

```typescript
rectangle(x: number, y: number, width: number, height: number): PathBuilder
```

### `solid`

```typescript
solid(): PathBuilder
```

### `strokeColor`

```typescript
strokeColor(color: Color): PathBuilder
```

Set the stroke color for subsequent path segments.

### `strokeWidth`

```typescript
strokeWidth(width: number): PathBuilder
```

Set the stroke width for subsequent path segments.

## Related guides

- [Working with Vector Graphics](../../working-with-vector-graphics)

[View source at `f7b7f2d7c269`](https://github.com/MenschMachine/pdfdancer-client-typescript/blob/f7b7f2d7c26956f37cac21c00f7efd8a93883931/src/path-builder.ts)

